# 直连版流程

QuizMate 账号鉴权 → 本机简历 → 逐 frame 扫描 → 拉取后台网申字段规则 → 后台网申模型规划 → 校验字段 ID 与置信度 → 控件逐项填写 → 动态扫描与回读 → 脱敏回传命中结果 → 用户检查提交。

PDF/DOCX 先在本机提取文字，再发送给模型生成结构化资料。截图辅助默认关闭。

本版没有 QuizMate 后台调用、个人访问密钥、服务账号或后台规则上报依赖。旧版后台代码没有部署，不参与本版运行。
