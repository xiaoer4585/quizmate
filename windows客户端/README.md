# Windows 客户端

- `QuizMate-Windows`：当前有效 Windows 客户端源码。
- `发布包/QuizMate-Windows-2026.8.10.exe`：当前正式安装包。

浏览器扩展已拆分到根目录 `扩展插件版`。旧 Windows 工程、QuizMate-Voice 分支、历史安装包和构建缓存已移入 `归档l历史代码正常不需要引用`，正式代码不依赖该归档目录。
