# 通用接口边界

通用底层提供以下能力：

- 账号：邮箱验证码、注册、登录、退出、会话续期和账号状态。
- 余额：积分账户、充值套餐、余额变更、幂等记账和流水审计。
- 支付：创建订单、查询订单、支付回调、签名校验和重复回调保护。
- 配置：邮件、支付、模型等运行配置的加密存储与读取。
- 安全：限流、请求体上限、管理员鉴权、日志脱敏和数据库备份。

各产品通过独立业务表或业务命名空间接入，不共享产品专属权益字段。C盘扩容继续在 `E:/ai项目/C盘扩容` 保留独立代码，只复用相同的账号、支付和安全设计。
