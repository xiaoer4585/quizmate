# 秋招助手与考试插件合并整理确认清单

日期：2026-08-12

目标根目录：`E:\ai项目\考试插件`

归档目录：`E:\ai项目\考试插件\归档l历史代码正常不需要引用`

## 已确认，可直接执行

1. 官网模块
   - 保留 `正式官网-quizmate.vip`。
   - 官网下载区只保留最新 Windows 安装包、Android 2026.07.31 安装包、最新扩展插件包和仍有效的操作手册。
   - 历史 Windows、Android、Mac 安装包移入归档。
   - Mac 下载入口改为“后续更新”，不再引用历史 Mac 文件。

2. 注册登陆模块
   - 将 `注册登陆模块（包含充值）` 重命名为 `注册登陆模块`。
   - 保留当前生产主后端 `阿里云后端-quizmate-api`。
   - 更新仍保留的部署脚本和文档路径，确保重命名后可构建、可发布。

3. windows客户端
   - 保留当前有效源码 `QuizMate-Windows`。
   - 保留最新安装包 `QuizMate-Windows-2026.8.10.exe`。
   - `QuizMate-Voice`、`release5` 至 `release34`、旧安装包、调试 asar、解压程序和其他历史构建移入归档。
   - `node_modules`、`out`、`release` 和 TypeScript 构建缓存属于可再生成内容，不作为有效源码保留。

4. 邀请注册机制
   - 将 `邀请和代理机制` 重命名为 `邀请注册机制`。
   - 保留设计说明；实际运行代码继续放在生产后端的 `referrals.ts` 等文件中，避免破坏后端编译边界。
   - 增加 README，标明文档与实际代码入口。

5. 域名售卖页
   - 保留现有独立 `index.html`，不并入官网代码。

6. mac客户端
   - 所有 Mac 源码、DMG、ZIP、解包文件和构建脚本移入归档。
   - 正式目录仅保留 README，标注“后续重新开发”。

7. 扩展插件版
   - 采用 `E:\ai项目\秋招助手\extension` 的 `2.2.0` 网申助手源码。
   - 原 `window客户端\浏览器插件\当前版本-2.0.0` 移入归档。
   - 新目录使用 `扩展插件版\QuizMate-网申助手`，并生成最新 ZIP 发布包。

8. 安卓端
   - 保留 `QuizMate-Android`，当前版本 `2026.07.31`，仅含笔试助手。
   - 保留 `QuizMate-Android-2026.07.31.apk`；更早 APK 移入归档。

9. 其他
   - 将仍有效的通用部署工具、配置说明、规范和根目录说明归类到 `其他`。
   - 临时压缩包、调试结果、上传日志、检查日志、解包目录和一次性实验脚本移入归档。
   - `.git`、`.gitignore`、`.agents`、`.codex` 等开发工具元数据继续留在根目录。

10. 合并收尾
    - 使用哈希核对所有跨目录移动。
    - 扫描正式目录，禁止代码、官网或部署脚本引用归档目录。
    - 对 Windows、后端、扩展插件和官网执行构建或静态校验。
    - 确认 `秋招助手` 内容已全部合并或归档后，移除空的旧目录。

## 需要确认的四项

### A. 阿里云统一入口

`阿里云统一入口-study-auth-api` 不是纯历史代码。它负责截图转存 OSS、兼容旧客户端和将请求转发给生产主 API，文档标注生产入口为 `https://api.quizmate.vip/study-auth-api`。

- A1（建议）：与生产主后端一起保留在 `注册登陆模块`。
- A2：归档，只保留生产主后端。此选择可能破坏现有截图转发和兼容链路。

`CloudBase兼容云函数` 已明确不参与生产，默认直接归档，不需确认。

### B. 职位雷达 Web

`官网模块\辅助页面-watch-web` 是 `watch.quizmate.vip` 的职位监控网页，支持职位匹配、监控包、来源和通知；它不是 Windows 客户端菜单。

- B1（建议）：继续保留在 `官网模块`，作为扩展插件之外的在线职位雷达。
- B2：归档，职位监控以后只保留在免费扩展插件里。

### C. 运营管理历史素材

`运营管理` 约 1.65 GB，包含宣传视频、品牌素材、教程素材和多批次软文。多数是不同渠道/日期的独立交付，不全是同一文件的旧版本。

- C1（建议）：保留所有独立批次，只归档 `_并发文件冲突待核对` 和可证明完全重复的副本。
- C2：只保留 2026-07-25 之后的内容，其余全部归档。
- C3：运营素材暂不整理，仅调整目录名称和明确重复项。

### D. 测试文档历史证据

`测试文档` 包含历史变更证据和回滚记录。它们不是运行代码，但对追溯问题有价值。

- D1（建议）：保留全部测试证据，整理索引，不以“旧版本”名义归档。
- D2：只保留当前 Windows、Android、后端的最新测试文档，旧证据全部归档。

## 回复格式

请回复四个选项，例如：`A1、B1、C1、D1`。
