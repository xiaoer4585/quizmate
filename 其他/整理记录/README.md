# 2026-08-12 合并整理记录

## 已确认的整理选择

- A1：保留生产后端 `阿里云后端-quizmate-api` 和 `阿里云统一入口-study-auth-api`。
- B2：`watch.quizmate.vip` 前端归档，不再由官网或客户端链接。
- C2：运营材料保留 2026-07-25 及以后内容。
- D2：测试文档仅保留当前有效验证文档；历史证据移入归档。

## 归档位置

本次历史代码、安装包、构建缓存和旧资料统一放在：

`归档l历史代码正常不需要引用/20260812-秋招助手与考试插件合并`

正式官网、后端、Windows 客户端、Android、浏览器扩展和部署工具均不引用该目录。

## 秋招助手源目录残留

`E:/ai项目/秋招助手` 是本次 Codex 任务的活动工作区，部分目录和文件被本机进程持有。已迁出全部可移动内容，仍可能保留：

- `tmp` 中 9 个旧 `.asar` 调试包，共 671,586,218 字节；归档中已有逐字节一致的副本。
- `extension` 空目录。
- `client/.git` 和 `mac-build/client` 两个拒绝访问的旧空壳。

这些残留不属于 Git 仓库，也不参与任何构建或部署。关闭 Codex 和相关 Electron 进程或重启后，可直接删除整个 `E:/ai项目/秋招助手` 目录。

## 记录文件

- `整理确认清单-20260812.md`：整理前的人工确认清单。
- `merge-reorganize.ps1`：本次目录迁移脚本，仅用于审计，不参与构建或部署。
